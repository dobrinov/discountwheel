<% if signed_request_data['user_id'].nil? %>
  <script>
    window.location.top = '<%= @auth_url %>';
  </script>
<% else %>
<% end %>

<%= signed_request_data.inspect %>

<%= link_to 'Spin', wheel_spin_path, remote: true %>

<%= image_tag 'mania.png', id: 'logo' %>
<%= image_tag 'pointer.png', id: 'pointer' %>

<script type="text/javascript">
  var wheel;

  $(document).ready(function(){
    // wheel = new Wheel();
  });
</script>