<table>
  <thead>
    <tr>
      <td>Name</td>
      <td>Delete</td>
    </tr>
  </thead>
  <tbody>
    <% @spins.each do |spin| %>
      <tr>
        <td>Name</td>
        <td><%= button_to('Delete', spin_path(spin), method: :delete, confirm: 'Are yoy sure?') %></td>
      </tr>
    <% end %>
  </tbody>
</table>